============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install django_auth_api
    $ pip install django_auth_api

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_auth_api
    $ pip install django_auth_api
